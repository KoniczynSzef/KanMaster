import FormWrapper from '@/components/Dashboard/ProjectForm/FormWrapper';
import React, { FC } from 'react';

interface Props {}

const page: FC<Props> = () => {
    return <FormWrapper />;
};

export default page;
