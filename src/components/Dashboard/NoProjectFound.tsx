import React, { FC } from 'react';

interface Props {}

const NoProjectFound: FC<Props> = ({}) => {
    return (
        <div>
            <p></p>
        </div>
    );
};

export default NoProjectFound;
